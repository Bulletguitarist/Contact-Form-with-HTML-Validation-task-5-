# Contact Form with HTML Validation

## 📌 Project Overview
This project demonstrates the creation of a **contact form using pure HTML and CSS** with **built-in HTML5 validation**.  
The form validates user input **without using JavaScript**, relying entirely on browser-level validation features.

The focus of this project is on **accessibility, clean UI, and correct form semantics**.

---

## 📂 Project Structure
task5/
│
├── contact.css
├── contact.html
└── README.md

---

## 🎯 Objectives
- Create a contact form using semantic HTML elements
- Apply correct input types (`text`, `email`, `tel`, `textarea`)
- Use HTML5 validation attributes
- Ensure accessibility with proper labels
- Group form fields logically
- Style the form using external CSS
- Understand default browser validation behavior

---

## 🧱 Technologies Used
- **HTML**
- **CSS**
- No JavaScript or external libraries

---

## 📝 Form Features
- Full Name input with minimum length validation
- Email input with built-in email format validation
- Phone number input with pattern-based validation
- Message textarea with maximum character limit
- Required field enforcement using `required` attribute
- Clean and accessible layout

---

## ♿ Accessibility & Best Practices
- `<label>` elements linked to inputs using `for` and `id`
- Logical grouping using `<fieldset>` and `<legend>`
- Clear placeholders and readable form layout
- External CSS for separation of concerns

---

## 🧪 Validation Behavior
- Empty fields trigger browser validation messages
- Invalid email formats are automatically detected
- Phone number pattern enforces 10-digit input
- Character limits prevent excessive input

All validation is handled by the browser **without JavaScript**.

---

## 🎨 Styling Highlights
- Gradient background
- Glassmorphism-style form container
- Responsive and centered layout
- Clear input fields and call-to-action button

---

## 🧠 Learning Outcomes
Through this project, I learned:
- How HTML validation works natively
- How to design accessible forms
- Importance of proper input types and attributes
- How to style forms for usability and clarity

---

## 👤 Author
**Jyotirmoy Mahapatra**  
  Intern- Elevate Labs
